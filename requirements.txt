# Единственная обязательная зависимость: отрисовка бланков и работа с изображениями.
# tkinter входит в стандартную поставку Python для Windows.
Pillow>=10.0

# Для разработки и тестов (не нужны для работы программы):
#   pytest>=8.0
#   pypdf>=4.0      — проверка корректности генерируемых PDF
#   pyflakes>=3.0   — статический анализ (ловит NameError до запуска)
